echo exec: csgoconfigs/keyboard

//          //
// keyboard //
//          //

bind v "+voicerecord"
bind p "crosshair_smoke"
bind e "+use_radar"                     // enable instruction mode and zoom the radar
bind b "use weapon_c4; drop; lastinv"   // drop the bomb
bind l "toggle cl_righthand 0 1"        // move the weapon between hands
bind space "+attack"                    // alternatively, duck_toggle
bind tab "+showscores_netgraph"         // show net_graph while tabulating

// IT layout: <
bind , clutch                           // disable voice chat and type in team chat

// ------------------------------ //

// radar zoom
bind o "incrementvar cl_radar_scale 0.2 0.8 0.5"

// nades
// zxcv = flash,smoke,nade,molotov
bind z "use weapon_flashbang"
bind x "use weapon_smokegrenade"
bind c "use weapon_hegrenade"
bind v "use weapon_molotov;use weapon_incgrenade"

// jumpthrow
bind alt "+jumpthrow" 